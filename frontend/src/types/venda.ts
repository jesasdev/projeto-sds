import { type } from "os"

export type VendaSum = {
    vendedorName: string;
    sum: number;
}

export type VendaSuccess={
    
        vendedorName: string;
        visitado: number;
        fechamento: number;
    
}